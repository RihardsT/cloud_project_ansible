---
dependency:
  name: galaxy
docker:
  containers:
    - name: cloud_project_ansible
      image: ubuntu
      image_version: 16.04
      privileged: true
      ansible_groups:
        - group1

verifier:
  name: testinfra

ansible:
  playbook: Test_playbook.yml
  user: root